language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"

sudo: false

matrix:
  fast_finish: true
  allow_failures:
    - python: "2.7"

script:
  - nosetests tests --with-coverage --cover-package=backcast --cover-erase --cover-html --rednose
